package test;

import org.junit.Before;
import org.junit.Test;
import static org.junit.Assert.*;

class should be saved in a separate file with the respective name and conventional file structure.

PositionTest.java
```java
import static org.junit.Assert.assertEquals;
import org.junit.Before;
import org.junit.Test;

public 